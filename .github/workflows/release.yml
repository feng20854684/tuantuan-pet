name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            platform: win32
            build-cmd: npm run package:win
            output-dir: release
            zip-name: tuantuan-pet-win32-x64.zip
          - os: macos-latest
            platform: darwin
            build-cmd: npm run package:mac
            output-dir: out
            zip-name: tuantuan-pet-darwin-arm64.zip
          - os: ubuntu-latest
            platform: linux
            build-cmd: npx electron-forge package
            output-dir: out
            zip-name: tuantuan-pet-linux-x64.zip

    runs-on: ${{ matrix.os }}
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run checks
        run: npm run check

      - name: Run unit tests
        run: npm test

      - name: Build
        env:
          PET_BUILD_AUTHORIZED: '1'
        run: ${{ matrix.build-cmd }}

      - name: Package output into zip
        shell: bash
        run: |
          cd ${{ matrix.output-dir }}
          zip -r ../${{ matrix.zip-name }} .
          cd ..

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.zip-name }}
          path: ${{ matrix.zip-name }}
          retention-days: 7

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            artifacts/**/*.zip
